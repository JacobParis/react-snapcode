import styled from 'styled-components';

const Button = styled.button`
    background: yellow;
    border: 4px solid;
    border-radius: 1rem;
    padding: 0.5rem 1rem;
    font-weight: bold;
    margin-top: 1rem;
    margin-bottom: 4rem;
`;

export default Button;